# A1 · Wav2Vec2 + LR 규제 비교

실행일: 2026-09-20. **Train 내부 3-fold만 완료했다.**
고정 Internal Validation 5,597개는 이번 실험에서 평가하지 않았다.

| C | Fold 1 | Fold 2 | Fold 3 | 평균 Accuracy | Train OOF 오답 |
|---|---:|---:|---:|---:|---:|
| **0.1** | 97.306713% | 97.467506% | 97.118735% | **97.297651%** | **605** |
| 1 · 기존 설정 | 97.038724% | 97.199518% | 97.051729% | 97.096657% | 650 |
| 10 | 96.569744% | 96.676940% | 96.716698% | 96.654461% | 749 |

`C=0.1`은 같은 fold의 기존 `C=1`보다 세 fold 모두 높았고 평균 **+0.200994%p**,
OOF 오답 **45개 순감소**였다. 99% 목표에는 여전히 격차가 있어 A2 MFCC 결합을
다음 단계로 진행한다. 이 결과를 기존 holdout 97.248526%와 직접 비교하지 않는다.

## 실행·검증

- 전체 입력 hash, call coverage, label/partition, segment 수 일치 확인.
- Train 22,388통화, 768차원, fold seed 42; Scaler는 fold 학습 부분만 fit.
- LR 설정: lbfgs, max_iter 1000, tol 1e-4, class_weight None, random_state 42.
- 9회 학습 완료, 수렴 경고 없음. 3개 후보 모두 동일한 fold 사용.
- 9회 fit 시간 합계 약 **18.30초**. 최초 입력 검증 11.5초, 재실행 검증 1.7초.
  환경 설치·검사 시간은 포함하지 않는다.
- 첫 fold 후 중단·재실행 시 해당 fold를 `RESUME`으로 건너뛰는 동작 확인.
- 전처리 누수·fold 겹침·중복 ID·불완전 후보 처리 테스트 4개 통과.

상세 fold 지표와 OOF 예측은 로컬 `results/run_01/`에 저장했다.
MFCC 결합(A2), RBF SVM(A3), 최종 후보 선정·Validation 평가는 아직 실행하지 않았다.
