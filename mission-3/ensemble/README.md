# 확률 앙상블

두 NPZ의 파일명·정답·클래스 순서가 같을 때만 섞는다. a의 비중은 0~1, 간격 0.05.
매 조합에서 증상별 threshold를 0.05~0.95, 간격 0.01로 탐색한다.
가장 높은 내부 Macro F1의 설정·예측을 저장하며 그 예측은 다시 입력으로 사용할 수 있다.

E2 = E1 + TF-IDF, 후속 = E3 + TF-IDF 또는 E3 + E2.
기존 E2 백업의 NPZ에는 파일명 배열이 없으므로 새 스크립트의 입력 형식과 다르다.
행 순서를 확인하지 않고 기존 확률과 새 확률을 섞지 않는다.
